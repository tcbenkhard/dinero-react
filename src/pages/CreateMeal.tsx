import React from "react";
import {CreateMealForm} from "../components/CreateMealForm";

export const CreateMeal = () => {
    return (
        <div>
            <CreateMealForm />
        </div>
    )
}